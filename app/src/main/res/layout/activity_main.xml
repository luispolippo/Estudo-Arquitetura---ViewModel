<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Contador"
        android:layout_marginTop="30dp"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/et_contador"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toBottomOf="@id/tv_label"
        app:layout_constraintStart_toStartOf="@id/tv_label"
        app:layout_constraintEnd_toEndOf="@id/tv_label"/>

    <Button
        android:id="@+id/btn_dados"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="Dados"
        app:layout_constraintTop_toBottomOf="@id/et_contador"
        app:layout_constraintStart_toStartOf="@id/et_contador"
        app:layout_constraintEnd_toEndOf="@id/et_contador"/>

    <Button
        android:id="@+id/btn_mostrar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="Mostrar"
        app:layout_constraintTop_toBottomOf="@id/btn_dados"
        app:layout_constraintStart_toStartOf="@id/btn_dados"
        app:layout_constraintEnd_toEndOf="@id/btn_dados"/>



</androidx.constraintlayout.widget.ConstraintLayout>